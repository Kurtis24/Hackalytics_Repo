APP_NAME=Hackalytics API
APP_VERSION=0.1.0
DEBUG=true
HOST=0.0.0.0
PORT=8000
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
DAYS_AHEAD=60

# ML model endpoint — POST to this URL to receive a prediction payload
ML_MODEL_URL=http://localhost:8001/predict

# ── Arbitrage Middleware — PRD v3 Volume Optimization ──────────────────────

# Confidence filter
MIN_CONFIDENCE=0.60

# Capital
BANKROLL=100000
KELLY_FRACTION=0.25
BANKROLL_CAP_PCT=0.10

# Profit floor — markets producing guaranteed profit below this USD amount are dropped
MIN_PROFIT_FLOOR=5

# Line movement sensitivity — USD depth per 1.0 unit of IP movement.
# Tune upward as execution data builds confidence. Lower if accounts get limited.
TRIGGER_THRESHOLD=0.005
# Calibrated for $100k bankroll on major US books (DraftKings/FanDuel/ESPNBet).
# NBA/NFL tier-1 markets handle millions per day — a $1k-$5k bet barely moves the line.
# Lower these if accounts get limited; raise if you see Kelly consistently binding.
SENSITIVITY_MONEYLINE=2000000
SENSITIVITY_SPREAD=1500000
SENSITIVITY_POINTS_TOTAL=1000000

# Scoring
PROFIT_CAP=0.05
ARB_RISK_CAP=0.10
EXPOSURE_CAP=200

# Risk weights — must sum to 1.0
WEIGHT_CONFIDENCE=0.40
WEIGHT_ARB_VALIDITY=0.35
WEIGHT_MKT_IMPACT=0.25
